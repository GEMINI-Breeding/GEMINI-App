# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# PyInstaller-built backend sidecar binaries (ignore build output)
/binaries/
# ...but keep the placeholder so Tauri's resource glob doesn't fail in dev
!/binaries/gemi-backend/placeholder.txt
